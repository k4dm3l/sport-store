// Register module aliases before importing any other module
import 'module-alias/register';
// Register TypeScript path mappings
import 'tsconfig-paths/register';

// Import the main application entry point
import './index';
